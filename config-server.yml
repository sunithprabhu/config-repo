spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sunithprabhu/config-repo/
          default-label: main
          clone-on-start: true
          username: sunithprabhu@gmail.com
          password: LordJesus@8179
server:
  port: 8881
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
